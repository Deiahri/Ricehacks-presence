# Copy to .env for local runs: node --env-file=.env server.mjs
# On Render, set these in the Web Service's Environment settings instead (.env is git-ignored).

# Port to listen on. Render sets this itself; leave it unset there.
PORT=8787

# Postgres connection string for accounts, friends, BP and workouts (Tiger Cloud: Service → Connect).
# Paste Tiger's string as-is: sslmode=require means encrypted but not certificate-checked, as in psql (see db.mjs).
# Leave unset to run without storage: sets are then logged as "skipped" and /api/* answers 503.
DATABASE_URL=postgres://tsdbadmin:your-password@your-service.tsdb.cloud.timescale.com:5432/tsdb?sslmode=require

# Voice coach (ElevenLabs Agents). The key stays on the server; the app only ever gets a one-session token.
# Set the key, then run `npm run coach:agent` once: it creates the agent and writes its id here. Leave unset to run without a coach.
ELEVENLABS_API_KEY=
ELEVENLABS_AGENT_ID=

# AI coaching (Gemini, Google AI Studio → Get API key): workout advice and the Progress recap. Leave the key unset to
# get simple rule-based text instead. GEMINI_MODEL is optional (default gemini-2.5-flash).
GEMINI_API_KEY=
GEMINI_MODEL=

# Sign-in (Clerk, Google). With the secret key set, every /api call and socket hello must carry a Clerk session token;
# leave it unset for local dev and `npm run smoke`, where the app's device id is the credential instead.
# Clerk dashboard → API keys. CLERK_AUTHORIZED_PARTIES is optional: the app's origin(s), comma-separated.
CLERK_SECRET_KEY=
CLERK_AUTHORIZED_PARTIES=

# Identity verification (Persona → Dashboard → API keys; a sandbox key works). With it set, only verified players rank on
# the global leaderboard and everyone else is tagged "Unverified". Leave unset to turn verification off (all count as
# verified). The app needs the matching VITE_PERSONA_TEMPLATE_ID / VITE_PERSONA_ENVIRONMENT_ID.
PERSONA_API_KEY=

# Comma-separated origins allowed to call /api/* from a browser. Default: * (auth is a header, not a cookie).
ALLOWED_ORIGINS=
